#ifndef WORKAROUND_BASIC_CLUSTER_MAPPER_TEST_HPP
#define WORKAROUND_BASIC_CLUSTER_MAPPER_TEST_HPP

/* This is a workaround to hide the C++ includes from
 * Unity's generate_test_runner.rb
*/

#ifdef __cplusplus
#include <string>
#include <map>
#include <vector>
#endif
#endif
