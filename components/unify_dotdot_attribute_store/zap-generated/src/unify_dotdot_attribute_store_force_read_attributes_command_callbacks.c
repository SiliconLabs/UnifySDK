/******************************************************************************
 * # License
 * <b>Copyright 2022 Silicon Laboratories Inc. www.silabs.com</b>
 ******************************************************************************
 * The licensor of this software is Silicon Laboratories Inc. Your use of this
 * software is governed by the terms of Silicon Labs Master Software License
 * Agreement (MSLA) available at
 * www.silabs.com/about-us/legal/master-software-license-agreement. This
 * software is distributed to you in Source Code format and is governed by the
 * sections of the MSLA applicable to Source Code.
 *
 *****************************************************************************/
// clang-format off
// This file is generated by ZCL Advanced Platform generator. Please don't edit manually.
// clang-format on
// Includes from this component
#include "unify_dotdot_attribute_store_configuration.h"
#include "unify_dotdot_attribute_store_helpers.h"
#include "unify_dotdot_attribute_store_force_read_attributes_command_callbacks.h"
#include "unify_dotdot_defined_attribute_types.h"

// Other includes
#include "zap-types.h"
#include "dotdot_mqtt.h"
#include "attribute_store.h"
#include "attribute_store_helper.h"

#include "sl_log.h"
#define LOG_TAG "unify_dotdot_attribute_store"

// clang-format off
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Basic
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_basic_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_basic_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_basic_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.zcl_version) {
    if (SL_STATUS_OK == dotdot_basic_zcl_version_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ZCLVersion under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.application_version) {
    if (SL_STATUS_OK == dotdot_basic_application_version_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ApplicationVersion under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.stack_version) {
    if (SL_STATUS_OK == dotdot_basic_stack_version_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::StackVersion under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.hw_version) {
    if (SL_STATUS_OK == dotdot_basic_hw_version_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::HWVersion under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.manufacturer_name) {
    if (SL_STATUS_OK == dotdot_basic_manufacturer_name_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ManufacturerName under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.model_identifier) {
    if (SL_STATUS_OK == dotdot_basic_model_identifier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ModelIdentifier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.date_code) {
    if (SL_STATUS_OK == dotdot_basic_date_code_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::DateCode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_source) {
    if (SL_STATUS_OK == dotdot_basic_power_source_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::PowerSource under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.generic_device_class) {
    if (SL_STATUS_OK == dotdot_basic_generic_device_class_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::GenericDevice-Class under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.generic_device_type) {
    if (SL_STATUS_OK == dotdot_basic_generic_device_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::GenericDevice-Type under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.product_code) {
    if (SL_STATUS_OK == dotdot_basic_product_code_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ProductCode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.producturl) {
    if (SL_STATUS_OK == dotdot_basic_producturl_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ProductURL under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.manufacturer_version_details) {
    if (SL_STATUS_OK == dotdot_basic_manufacturer_version_details_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ManufacturerVersionDetails under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.serial_number) {
    if (SL_STATUS_OK == dotdot_basic_serial_number_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::SerialNumber under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.product_label) {
    if (SL_STATUS_OK == dotdot_basic_product_label_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::ProductLabel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.location_description) {
    if (SL_STATUS_OK == dotdot_basic_location_description_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::LocationDescription under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_environment) {
    if (SL_STATUS_OK == dotdot_basic_physical_environment_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::PhysicalEnvironment under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.device_enabled) {
    if (SL_STATUS_OK == dotdot_basic_device_enabled_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::DeviceEnabled under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.alarm_mask) {
    if (SL_STATUS_OK == dotdot_basic_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.disable_local_config) {
    if (SL_STATUS_OK == dotdot_basic_disable_local_config_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::DisableLocalConfig under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.sw_buildid) {
    if (SL_STATUS_OK == dotdot_basic_sw_buildid_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Basic::SWBuildID under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PowerConfiguration
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_power_configuration_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_power_configuration_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_power_configuration_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.mains_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mains_frequency) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_frequency_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsFrequency under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mains_alarm_mask) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsAlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mains_voltage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_voltage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsVoltageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mains_voltage_max_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_voltage_max_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsVoltageMaxThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mains_voltage_dwell_trip_point) {
    if (SL_STATUS_OK == dotdot_power_configuration_mains_voltage_dwell_trip_point_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::MainsVoltageDwellTripPoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_percentage_remaining) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_percentage_remaining_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryPercentageRemaining under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_manufacturer) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_manufacturer_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryManufacturer under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_size) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_size_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatterySize under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.batterya_hr_rating) {
    if (SL_STATUS_OK == dotdot_power_configuration_batterya_hr_rating_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryAHrRating under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_quantity) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_quantity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryQuantity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_rated_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_rated_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryRatedVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_alarm_mask) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryAlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_voltage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_voltage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryVoltageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_voltage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_voltage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryVoltageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_voltage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_voltage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryVoltageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_voltage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_voltage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryVoltageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_percentage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_percentage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryPercentageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_percentage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_percentage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryPercentageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_percentage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_percentage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryPercentageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_percentage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_percentage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryPercentageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery_alarm_state) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery_alarm_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::BatteryAlarmState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2Voltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_percentage_remaining) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_percentage_remaining_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2PercentageRemaining under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_manufacturer) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_manufacturer_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2Manufacturer under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_size) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_size_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2Size under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2a_hr_rating) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2a_hr_rating_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2AHrRating under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_quantity) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_quantity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2Quantity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_rated_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_rated_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2RatedVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_alarm_mask) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_voltage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_voltage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2VoltageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_voltage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_voltage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2VoltageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_voltage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_voltage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2VoltageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_voltage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_voltage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2VoltageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_percentage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_percentage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2PercentageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_percentage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_percentage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2PercentageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_percentage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_percentage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2PercentageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_percentage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_percentage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2PercentageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery2_alarm_state) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery2_alarm_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery2AlarmState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3Voltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_percentage_remaining) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_percentage_remaining_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3PercentageRemaining under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_manufacturer) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_manufacturer_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3Manufacturer under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_size) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_size_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3Size under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3a_hr_rating) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3a_hr_rating_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3AHrRating under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_quantity) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_quantity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3Quantity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_rated_voltage) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_rated_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3RatedVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_alarm_mask) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_voltage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_voltage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3VoltageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_voltage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_voltage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3VoltageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_voltage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_voltage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3VoltageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_voltage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_voltage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3VoltageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_percentage_min_threshold) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_percentage_min_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3PercentageMinThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_percentage_threshold1) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_percentage_threshold1_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3PercentageThreshold1 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_percentage_threshold2) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_percentage_threshold2_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3PercentageThreshold2 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_percentage_threshold3) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_percentage_threshold3_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3PercentageThreshold3 under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.battery3_alarm_state) {
    if (SL_STATUS_OK == dotdot_power_configuration_battery3_alarm_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PowerConfiguration::Battery3AlarmState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster DeviceTemperatureConfiguration
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_device_temperature_configuration_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_device_temperature_configuration_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_device_temperature_configuration_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.current_temperature) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_current_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::CurrentTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_temp_experienced) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_min_temp_experienced_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::MinTempExperienced under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_temp_experienced) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_max_temp_experienced_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::MaxTempExperienced under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.over_temp_total_dwell) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_over_temp_total_dwell_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::OverTempTotalDwell under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.device_temp_alarm_mask) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_device_temp_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::DeviceTempAlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.low_temp_threshold) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_low_temp_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::LowTempThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.high_temp_threshold) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_high_temp_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::HighTempThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.low_temp_dwell_trip_point) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_low_temp_dwell_trip_point_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::LowTempDwellTripPoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.high_temp_dwell_trip_point) {
    if (SL_STATUS_OK == dotdot_device_temperature_configuration_high_temp_dwell_trip_point_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DeviceTemperatureConfiguration::HighTempDwellTripPoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Identify
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_identify_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_identify_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_identify_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.identify_time) {
    if (SL_STATUS_OK == dotdot_identify_identify_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Identify::IdentifyTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Groups
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_groups_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_groups_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_groups_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.name_support) {
    if (SL_STATUS_OK == dotdot_groups_name_support_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Groups::NameSupport under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Scenes
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_scenes_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_scenes_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_scenes_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.scene_count) {
    if (SL_STATUS_OK == dotdot_scenes_scene_count_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::SceneCount under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_scene) {
    if (SL_STATUS_OK == dotdot_scenes_current_scene_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::CurrentScene under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_group) {
    if (SL_STATUS_OK == dotdot_scenes_current_group_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::CurrentGroup under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.scene_valid) {
    if (SL_STATUS_OK == dotdot_scenes_scene_valid_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::SceneValid under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.name_support) {
    if (SL_STATUS_OK == dotdot_scenes_name_support_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::NameSupport under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.last_configured_by) {
    if (SL_STATUS_OK == dotdot_scenes_last_configured_by_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::LastConfiguredBy under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.scene_table) {
    if (SL_STATUS_OK == dotdot_scenes_scene_table_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Scenes::SceneTable under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster OnOff
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_on_off_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_on_off_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_on_off_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.on_off) {
    if (SL_STATUS_OK == dotdot_on_off_on_off_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OnOff::OnOff under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.global_scene_control) {
    if (SL_STATUS_OK == dotdot_on_off_global_scene_control_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OnOff::GlobalSceneControl under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.on_time) {
    if (SL_STATUS_OK == dotdot_on_off_on_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OnOff::OnTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.off_wait_time) {
    if (SL_STATUS_OK == dotdot_on_off_off_wait_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OnOff::OffWaitTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.start_up_on_off) {
    if (SL_STATUS_OK == dotdot_on_off_start_up_on_off_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OnOff::StartUpOnOff under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Level
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_level_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_level_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_level_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.current_level) {
    if (SL_STATUS_OK == dotdot_level_current_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::CurrentLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.remaining_time) {
    if (SL_STATUS_OK == dotdot_level_remaining_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::RemainingTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_level) {
    if (SL_STATUS_OK == dotdot_level_min_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::MinLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_level) {
    if (SL_STATUS_OK == dotdot_level_max_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::MaxLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_frequency) {
    if (SL_STATUS_OK == dotdot_level_current_frequency_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::CurrentFrequency under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_frequency) {
    if (SL_STATUS_OK == dotdot_level_min_frequency_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::MinFrequency under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_frequency) {
    if (SL_STATUS_OK == dotdot_level_max_frequency_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::MaxFrequency under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.options) {
    if (SL_STATUS_OK == dotdot_level_options_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::Options under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.on_off_transition_time) {
    if (SL_STATUS_OK == dotdot_level_on_off_transition_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::OnOffTransitionTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.on_level) {
    if (SL_STATUS_OK == dotdot_level_on_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::OnLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.on_transition_time) {
    if (SL_STATUS_OK == dotdot_level_on_transition_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::OnTransitionTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.off_transition_time) {
    if (SL_STATUS_OK == dotdot_level_off_transition_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::OffTransitionTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.default_move_rate) {
    if (SL_STATUS_OK == dotdot_level_default_move_rate_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::DefaultMoveRate under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.start_up_current_level) {
    if (SL_STATUS_OK == dotdot_level_start_up_current_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Level::StartUpCurrentLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Alarms
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_alarms_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_alarms_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_alarms_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.alarm_count) {
    if (SL_STATUS_OK == dotdot_alarms_alarm_count_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Alarms::AlarmCount under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Time
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_time_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_time_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_time_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.time) {
    if (SL_STATUS_OK == dotdot_time_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::Time under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.time_status) {
    if (SL_STATUS_OK == dotdot_time_time_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::TimeStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.time_zone) {
    if (SL_STATUS_OK == dotdot_time_time_zone_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::TimeZone under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dst_start) {
    if (SL_STATUS_OK == dotdot_time_dst_start_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::DstStart under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dst_end) {
    if (SL_STATUS_OK == dotdot_time_dst_end_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::DstEnd under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dst_shift) {
    if (SL_STATUS_OK == dotdot_time_dst_shift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::DstShift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.standard_time) {
    if (SL_STATUS_OK == dotdot_time_standard_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::StandardTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.local_time) {
    if (SL_STATUS_OK == dotdot_time_local_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::LocalTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.last_set_time) {
    if (SL_STATUS_OK == dotdot_time_last_set_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::LastSetTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.valid_until_time) {
    if (SL_STATUS_OK == dotdot_time_valid_until_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Time::ValidUntilTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PollControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_poll_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_poll_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_poll_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.check_in_interval) {
    if (SL_STATUS_OK == dotdot_poll_control_check_in_interval_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::CheckInInterval under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.long_poll_interval) {
    if (SL_STATUS_OK == dotdot_poll_control_long_poll_interval_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::LongPollInterval under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.short_poll_interval) {
    if (SL_STATUS_OK == dotdot_poll_control_short_poll_interval_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::ShortPollInterval under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.fast_poll_timeout) {
    if (SL_STATUS_OK == dotdot_poll_control_fast_poll_timeout_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::FastPollTimeout under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.check_in_interval_min) {
    if (SL_STATUS_OK == dotdot_poll_control_check_in_interval_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::CheckInIntervalMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.long_poll_interval_min) {
    if (SL_STATUS_OK == dotdot_poll_control_long_poll_interval_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::LongPollIntervalMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.fast_poll_timeout_max) {
    if (SL_STATUS_OK == dotdot_poll_control_fast_poll_timeout_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PollControl::FastPollTimeoutMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ShadeConfiguration
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_shade_configuration_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_shade_configuration_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_shade_configuration_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.physical_closed_limit) {
    if (SL_STATUS_OK == dotdot_shade_configuration_physical_closed_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ShadeConfiguration::PhysicalClosedLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.motor_step_size) {
    if (SL_STATUS_OK == dotdot_shade_configuration_motor_step_size_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ShadeConfiguration::MotorStepSize under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.status) {
    if (SL_STATUS_OK == dotdot_shade_configuration_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ShadeConfiguration::Status under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.closed_limit) {
    if (SL_STATUS_OK == dotdot_shade_configuration_closed_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ShadeConfiguration::ClosedLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mode) {
    if (SL_STATUS_OK == dotdot_shade_configuration_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ShadeConfiguration::Mode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster DoorLock
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_door_lock_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_door_lock_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_door_lock_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.lock_state) {
    if (SL_STATUS_OK == dotdot_door_lock_lock_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::LockState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lock_type) {
    if (SL_STATUS_OK == dotdot_door_lock_lock_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::LockType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.actuator_enabled) {
    if (SL_STATUS_OK == dotdot_door_lock_actuator_enabled_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::ActuatorEnabled under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.door_state) {
    if (SL_STATUS_OK == dotdot_door_lock_door_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::DoorState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.door_open_events) {
    if (SL_STATUS_OK == dotdot_door_lock_door_open_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::DoorOpenEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.door_closed_events) {
    if (SL_STATUS_OK == dotdot_door_lock_door_closed_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::DoorClosedEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.open_period) {
    if (SL_STATUS_OK == dotdot_door_lock_open_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::OpenPeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_log_records_supported) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_log_records_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfLogRecordsSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_total_users_supported) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_total_users_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfTotalUsersSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_pin_users_supported) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_pin_users_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfPINUsersSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_rfid_users_supported) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_rfid_users_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfRFIDUsersSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_week_day_schedules_supported_per_user) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_week_day_schedules_supported_per_user_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfWeekDaySchedulesSupportedPerUser under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_year_day_schedules_supported_per_user) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_year_day_schedules_supported_per_user_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfYearDaySchedulesSupportedPerUser under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_holiday_schedules_supported) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_holiday_schedules_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfHolidaySchedulesSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_pin_code_length) {
    if (SL_STATUS_OK == dotdot_door_lock_max_pin_code_length_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::MaxPINCodeLength under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_pin_code_length) {
    if (SL_STATUS_OK == dotdot_door_lock_min_pin_code_length_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::MinPINCodeLength under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_rfid_code_length) {
    if (SL_STATUS_OK == dotdot_door_lock_max_rfid_code_length_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::MaxRFIDCodeLength under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_rfid_code_length) {
    if (SL_STATUS_OK == dotdot_door_lock_min_rfid_code_length_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::MinRFIDCodeLength under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.credential_rules_support) {
    if (SL_STATUS_OK == dotdot_door_lock_credential_rules_support_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::CredentialRulesSupport under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_credentials_supported_per_user) {
    if (SL_STATUS_OK == dotdot_door_lock_number_of_credentials_supported_per_user_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::NumberOfCredentialsSupportedPerUser under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enable_logging) {
    if (SL_STATUS_OK == dotdot_door_lock_enable_logging_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::EnableLogging under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.language) {
    if (SL_STATUS_OK == dotdot_door_lock_language_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::Language under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.led_settings) {
    if (SL_STATUS_OK == dotdot_door_lock_led_settings_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::LEDSettings under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.auto_relock_time) {
    if (SL_STATUS_OK == dotdot_door_lock_auto_relock_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::AutoRelockTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.sound_volume) {
    if (SL_STATUS_OK == dotdot_door_lock_sound_volume_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::SoundVolume under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.operating_mode) {
    if (SL_STATUS_OK == dotdot_door_lock_operating_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::OperatingMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.supported_operating_modes) {
    if (SL_STATUS_OK == dotdot_door_lock_supported_operating_modes_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::SupportedOperatingModes under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.default_configuration_register) {
    if (SL_STATUS_OK == dotdot_door_lock_default_configuration_register_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::DefaultConfigurationRegister under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enable_local_programming) {
    if (SL_STATUS_OK == dotdot_door_lock_enable_local_programming_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::EnableLocalProgramming under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enable_one_touch_locking) {
    if (SL_STATUS_OK == dotdot_door_lock_enable_one_touch_locking_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::EnableOneTouchLocking under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enable_inside_statusled) {
    if (SL_STATUS_OK == dotdot_door_lock_enable_inside_statusled_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::EnableInsideStatusLED under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enable_privacy_mode_button) {
    if (SL_STATUS_OK == dotdot_door_lock_enable_privacy_mode_button_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::EnablePrivacyModeButton under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.local_programming_features) {
    if (SL_STATUS_OK == dotdot_door_lock_local_programming_features_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::LocalProgrammingFeatures under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.wrong_code_entry_limit) {
    if (SL_STATUS_OK == dotdot_door_lock_wrong_code_entry_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::WrongCodeEntryLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.user_code_temporary_disable_time) {
    if (SL_STATUS_OK == dotdot_door_lock_user_code_temporary_disable_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::UserCodeTemporaryDisableTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.send_pin_over_the_air) {
    if (SL_STATUS_OK == dotdot_door_lock_send_pin_over_the_air_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::SendPINOverTheAir under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.require_pi_nfor_rf_operation) {
    if (SL_STATUS_OK == dotdot_door_lock_require_pi_nfor_rf_operation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::RequirePINforRFOperation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.security_level) {
    if (SL_STATUS_OK == dotdot_door_lock_security_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::SecurityLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.expiring_user_timeout) {
    if (SL_STATUS_OK == dotdot_door_lock_expiring_user_timeout_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::ExpiringUserTimeout under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.alarm_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.keypad_operation_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_keypad_operation_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::KeypadOperationEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rf_operation_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_rf_operation_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::RFOperationEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.manual_operation_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_manual_operation_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::ManualOperationEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rfid_operation_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_rfid_operation_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::RFIDOperationEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.keypad_programming_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_keypad_programming_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::KeypadProgrammingEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rf_programming_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_rf_programming_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::RFProgrammingEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rfid_programming_event_mask) {
    if (SL_STATUS_OK == dotdot_door_lock_rfid_programming_event_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::RFIDProgrammingEventMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.feature_map) {
    if (SL_STATUS_OK == dotdot_door_lock_feature_map_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DoorLock::FeatureMap under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster WindowCovering
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_window_covering_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_window_covering_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_window_covering_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.window_covering_type) {
    if (SL_STATUS_OK == dotdot_window_covering_window_covering_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::WindowCoveringType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_closed_limit_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_physical_closed_limit_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::PhysicalClosedLimitLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_closed_limit_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_physical_closed_limit_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::PhysicalClosedLimitTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_position_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_current_position_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::CurrentPositionLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_position_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_current_position_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::CurrentPositionTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_actuations_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_number_of_actuations_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::NumberOfActuationsLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_actuations_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_number_of_actuations_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::NumberOfActuationsTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.config_or_status) {
    if (SL_STATUS_OK == dotdot_window_covering_config_or_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::ConfigOrStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_position_lift_percentage) {
    if (SL_STATUS_OK == dotdot_window_covering_current_position_lift_percentage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::CurrentPositionLiftPercentage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_position_tilt_percentage) {
    if (SL_STATUS_OK == dotdot_window_covering_current_position_tilt_percentage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::CurrentPositionTiltPercentage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.installed_open_limit_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_installed_open_limit_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::InstalledOpenLimitLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.installed_closed_limit_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_installed_closed_limit_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::InstalledClosedLimitLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.installed_open_limit_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_installed_open_limit_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::InstalledOpenLimitTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.installed_closed_limit_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_installed_closed_limit_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::InstalledClosedLimitTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.velocity_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_velocity_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::VelocityLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.acceleration_time_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_acceleration_time_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::AccelerationTimeLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.deceleration_time_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_deceleration_time_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::DecelerationTimeLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mode) {
    if (SL_STATUS_OK == dotdot_window_covering_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::Mode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.intermediate_setpoints_lift) {
    if (SL_STATUS_OK == dotdot_window_covering_intermediate_setpoints_lift_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::IntermediateSetpointsLift under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.intermediate_setpoints_tilt) {
    if (SL_STATUS_OK == dotdot_window_covering_intermediate_setpoints_tilt_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindowCovering::IntermediateSetpointsTilt under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster BarrierControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_barrier_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_barrier_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_barrier_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.moving_state) {
    if (SL_STATUS_OK == dotdot_barrier_control_moving_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::MovingState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.safety_status) {
    if (SL_STATUS_OK == dotdot_barrier_control_safety_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::SafetyStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.capabilities) {
    if (SL_STATUS_OK == dotdot_barrier_control_capabilities_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::Capabilities under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.open_events) {
    if (SL_STATUS_OK == dotdot_barrier_control_open_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::OpenEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.close_events) {
    if (SL_STATUS_OK == dotdot_barrier_control_close_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::CloseEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.command_open_events) {
    if (SL_STATUS_OK == dotdot_barrier_control_command_open_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::CommandOpenEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.command_close_events) {
    if (SL_STATUS_OK == dotdot_barrier_control_command_close_events_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::CommandCloseEvents under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.open_period) {
    if (SL_STATUS_OK == dotdot_barrier_control_open_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::OpenPeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.close_period) {
    if (SL_STATUS_OK == dotdot_barrier_control_close_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::ClosePeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.barrier_position) {
    if (SL_STATUS_OK == dotdot_barrier_control_barrier_position_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BarrierControl::BarrierPosition under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PumpConfigurationAndControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_pump_configuration_and_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_pump_configuration_and_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_pump_configuration_and_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.max_pressure) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_pressure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxPressure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_speed) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_speed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxSpeed under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_flow) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_flow_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxFlow under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_const_pressure) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_min_const_pressure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MinConstPressure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_const_pressure) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_const_pressure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxConstPressure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_comp_pressure) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_min_comp_pressure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MinCompPressure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_comp_pressure) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_comp_pressure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxCompPressure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_const_speed) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_min_const_speed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MinConstSpeed under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_const_speed) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_const_speed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxConstSpeed under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_const_flow) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_min_const_flow_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MinConstFlow under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_const_flow) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_const_flow_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxConstFlow under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_const_temp) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_min_const_temp_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MinConstTemp under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_const_temp) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_max_const_temp_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::MaxConstTemp under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pump_status) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_pump_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::PumpStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.effective_operation_mode) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_effective_operation_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::EffectiveOperationMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.effective_control_mode) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_effective_control_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::EffectiveControlMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.capacity) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_capacity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::Capacity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.speed) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_speed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::Speed under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lifetime_running_hours) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_lifetime_running_hours_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::LifetimeRunningHours under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::Power under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lifetime_energy_consumed) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_lifetime_energy_consumed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::LifetimeEnergyConsumed under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.operation_mode) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_operation_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::OperationMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.control_mode) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_control_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::ControlMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.alarm_mask) {
    if (SL_STATUS_OK == dotdot_pump_configuration_and_control_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PumpConfigurationAndControl::AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Thermostat
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_thermostat_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_thermostat_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_thermostat_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.local_temperature) {
    if (SL_STATUS_OK == dotdot_thermostat_local_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::LocalTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.outdoor_temperature) {
    if (SL_STATUS_OK == dotdot_thermostat_outdoor_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OutdoorTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupancy) {
    if (SL_STATUS_OK == dotdot_thermostat_occupancy_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::Occupancy under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.abs_min_heat_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_abs_min_heat_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::AbsMinHeatSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.abs_max_heat_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_abs_max_heat_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::AbsMaxHeatSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.abs_min_cool_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_abs_min_cool_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::AbsMinCoolSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.abs_max_cool_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_abs_max_cool_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::AbsMaxCoolSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pi_cooling_demand) {
    if (SL_STATUS_OK == dotdot_thermostat_pi_cooling_demand_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::PICoolingDemand under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pi_heating_demand) {
    if (SL_STATUS_OK == dotdot_thermostat_pi_heating_demand_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::PIHeatingDemand under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.hvac_system_type_configuration) {
    if (SL_STATUS_OK == dotdot_thermostat_hvac_system_type_configuration_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::HVACSystemTypeConfiguration under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.local_temperature_calibration) {
    if (SL_STATUS_OK == dotdot_thermostat_local_temperature_calibration_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::LocalTemperatureCalibration under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupied_cooling_setpoint) {
    if (SL_STATUS_OK == dotdot_thermostat_occupied_cooling_setpoint_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OccupiedCoolingSetpoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupied_heating_setpoint) {
    if (SL_STATUS_OK == dotdot_thermostat_occupied_heating_setpoint_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OccupiedHeatingSetpoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unoccupied_cooling_setpoint) {
    if (SL_STATUS_OK == dotdot_thermostat_unoccupied_cooling_setpoint_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::UnoccupiedCoolingSetpoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unoccupied_heating_setpoint) {
    if (SL_STATUS_OK == dotdot_thermostat_unoccupied_heating_setpoint_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::UnoccupiedHeatingSetpoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_heat_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_min_heat_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::MinHeatSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_heat_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_max_heat_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::MaxHeatSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_cool_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_min_cool_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::MinCoolSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_cool_setpoint_limit) {
    if (SL_STATUS_OK == dotdot_thermostat_max_cool_setpoint_limit_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::MaxCoolSetpointLimit under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_setpoint_dead_band) {
    if (SL_STATUS_OK == dotdot_thermostat_min_setpoint_dead_band_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::MinSetpointDeadBand under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.remote_sensing) {
    if (SL_STATUS_OK == dotdot_thermostat_remote_sensing_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::RemoteSensing under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.control_sequence_of_operation) {
    if (SL_STATUS_OK == dotdot_thermostat_control_sequence_of_operation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ControlSequenceOfOperation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.system_mode) {
    if (SL_STATUS_OK == dotdot_thermostat_system_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::SystemMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.alarm_mask) {
    if (SL_STATUS_OK == dotdot_thermostat_alarm_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::AlarmMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.thermostat_running_mode) {
    if (SL_STATUS_OK == dotdot_thermostat_thermostat_running_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ThermostatRunningMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.start_of_week) {
    if (SL_STATUS_OK == dotdot_thermostat_start_of_week_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::StartOfWeek under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_weekly_transitions) {
    if (SL_STATUS_OK == dotdot_thermostat_number_of_weekly_transitions_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::NumberOfWeeklyTransitions under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_daily_transitions) {
    if (SL_STATUS_OK == dotdot_thermostat_number_of_daily_transitions_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::NumberOfDailyTransitions under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.temperature_setpoint_hold) {
    if (SL_STATUS_OK == dotdot_thermostat_temperature_setpoint_hold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::TemperatureSetpointHold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.temperature_setpoint_hold_duration) {
    if (SL_STATUS_OK == dotdot_thermostat_temperature_setpoint_hold_duration_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::TemperatureSetpointHoldDuration under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.thermostat_programming_operation_mode) {
    if (SL_STATUS_OK == dotdot_thermostat_thermostat_programming_operation_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ThermostatProgrammingOperationMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.thermostat_running_state) {
    if (SL_STATUS_OK == dotdot_thermostat_thermostat_running_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ThermostatRunningState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.setpoint_change_source) {
    if (SL_STATUS_OK == dotdot_thermostat_setpoint_change_source_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::SetpointChangeSource under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.setpoint_change_amount) {
    if (SL_STATUS_OK == dotdot_thermostat_setpoint_change_amount_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::SetpointChangeAmount under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.setpoint_change_source_timestamp) {
    if (SL_STATUS_OK == dotdot_thermostat_setpoint_change_source_timestamp_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::SetpointChangeSourceTimestamp under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupied_setback) {
    if (SL_STATUS_OK == dotdot_thermostat_occupied_setback_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OccupiedSetback under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupied_setback_min) {
    if (SL_STATUS_OK == dotdot_thermostat_occupied_setback_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OccupiedSetbackMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupied_setback_max) {
    if (SL_STATUS_OK == dotdot_thermostat_occupied_setback_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::OccupiedSetbackMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unoccupied_setback) {
    if (SL_STATUS_OK == dotdot_thermostat_unoccupied_setback_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::UnoccupiedSetback under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unoccupied_setback_min) {
    if (SL_STATUS_OK == dotdot_thermostat_unoccupied_setback_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::UnoccupiedSetbackMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unoccupied_setback_max) {
    if (SL_STATUS_OK == dotdot_thermostat_unoccupied_setback_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::UnoccupiedSetbackMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.emergency_heat_delta) {
    if (SL_STATUS_OK == dotdot_thermostat_emergency_heat_delta_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::EmergencyHeatDelta under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_type) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_capacity) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_capacity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACCapacity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_refrigerant_type) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_refrigerant_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACRefrigerantType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_compressor_type) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_compressor_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACCompressorType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_error_code) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_error_code_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACErrorCode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_louver_position) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_louver_position_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACLouverPosition under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_coil_temperature) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_coil_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACCoilTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_capacity_format) {
    if (SL_STATUS_OK == dotdot_thermostat_ac_capacity_format_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Thermostat::ACCapacityFormat under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster FanControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_fan_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_fan_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_fan_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.fan_mode) {
    if (SL_STATUS_OK == dotdot_fan_control_fan_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FanControl::FanMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.fan_mode_sequence) {
    if (SL_STATUS_OK == dotdot_fan_control_fan_mode_sequence_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FanControl::FanModeSequence under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster DehumidificationControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_dehumidification_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_dehumidification_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_dehumidification_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.relative_humidity) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_relative_humidity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::RelativeHumidity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dehumidification_cooling) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_dehumidification_cooling_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::DehumidificationCooling under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rh_dehumidification_setpoint) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_rh_dehumidification_setpoint_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::RHDehumidificationSetpoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.relative_humidity_mode) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_relative_humidity_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::RelativeHumidityMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dehumidification_lockout) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_dehumidification_lockout_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::DehumidificationLockout under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dehumidification_hysteresis) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_dehumidification_hysteresis_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::DehumidificationHysteresis under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dehumidification_max_cool) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_dehumidification_max_cool_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::DehumidificationMaxCool under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.relative_humidity_display) {
    if (SL_STATUS_OK == dotdot_dehumidification_control_relative_humidity_display_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of DehumidificationControl::RelativeHumidityDisplay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ThermostatUserInterfaceConfiguration
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_thermostat_user_interface_configuration_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_thermostat_user_interface_configuration_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_thermostat_user_interface_configuration_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.temperature_display_mode) {
    if (SL_STATUS_OK == dotdot_thermostat_user_interface_configuration_temperature_display_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ThermostatUserInterfaceConfiguration::TemperatureDisplayMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.keypad_lockout) {
    if (SL_STATUS_OK == dotdot_thermostat_user_interface_configuration_keypad_lockout_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ThermostatUserInterfaceConfiguration::KeypadLockout under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.schedule_programming_visibility) {
    if (SL_STATUS_OK == dotdot_thermostat_user_interface_configuration_schedule_programming_visibility_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ThermostatUserInterfaceConfiguration::ScheduleProgrammingVisibility under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ColorControl
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_color_control_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_color_control_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_color_control_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.current_hue) {
    if (SL_STATUS_OK == dotdot_color_control_current_hue_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CurrentHue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_saturation) {
    if (SL_STATUS_OK == dotdot_color_control_current_saturation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CurrentSaturation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.remaining_time) {
    if (SL_STATUS_OK == dotdot_color_control_remaining_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::RemainingTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.currentx) {
    if (SL_STATUS_OK == dotdot_color_control_currentx_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CurrentX under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.currenty) {
    if (SL_STATUS_OK == dotdot_color_control_currenty_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CurrentY under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.drift_compensation) {
    if (SL_STATUS_OK == dotdot_color_control_drift_compensation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::DriftCompensation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.compensation_text) {
    if (SL_STATUS_OK == dotdot_color_control_compensation_text_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CompensationText under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_temperature_mireds) {
    if (SL_STATUS_OK == dotdot_color_control_color_temperature_mireds_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorTemperatureMireds under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_mode) {
    if (SL_STATUS_OK == dotdot_color_control_color_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.options) {
    if (SL_STATUS_OK == dotdot_color_control_options_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Options under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_primaries) {
    if (SL_STATUS_OK == dotdot_color_control_number_of_primaries_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::NumberOfPrimaries under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary1x) {
    if (SL_STATUS_OK == dotdot_color_control_primary1x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary1X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary1y) {
    if (SL_STATUS_OK == dotdot_color_control_primary1y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary1Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary1_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary1_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary1Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary2x) {
    if (SL_STATUS_OK == dotdot_color_control_primary2x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary2X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary2y) {
    if (SL_STATUS_OK == dotdot_color_control_primary2y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary2Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary2_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary2_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary2Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary3x) {
    if (SL_STATUS_OK == dotdot_color_control_primary3x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary3X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary3y) {
    if (SL_STATUS_OK == dotdot_color_control_primary3y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary3Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary3_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary3_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary3Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary4x) {
    if (SL_STATUS_OK == dotdot_color_control_primary4x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary4X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary4y) {
    if (SL_STATUS_OK == dotdot_color_control_primary4y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary4Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary4_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary4_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary4Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary5x) {
    if (SL_STATUS_OK == dotdot_color_control_primary5x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary5X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary5y) {
    if (SL_STATUS_OK == dotdot_color_control_primary5y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary5Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary5_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary5_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary5Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary6x) {
    if (SL_STATUS_OK == dotdot_color_control_primary6x_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary6X under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary6y) {
    if (SL_STATUS_OK == dotdot_color_control_primary6y_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary6Y under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.primary6_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_primary6_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::Primary6Intensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.white_pointx) {
    if (SL_STATUS_OK == dotdot_color_control_white_pointx_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::WhitePointX under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.white_pointy) {
    if (SL_STATUS_OK == dotdot_color_control_white_pointy_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::WhitePointY under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointrx) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointrx_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointRX under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointry) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointry_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointRY under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointr_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointr_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointRIntensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointgx) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointgx_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointGX under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointgy) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointgy_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointGY under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointg_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointg_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointGIntensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointbx) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointbx_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointBX under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointby) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointby_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointBY under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_pointb_intensity) {
    if (SL_STATUS_OK == dotdot_color_control_color_pointb_intensity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorPointBIntensity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enhanced_current_hue) {
    if (SL_STATUS_OK == dotdot_color_control_enhanced_current_hue_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::EnhancedCurrentHue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.enhanced_color_mode) {
    if (SL_STATUS_OK == dotdot_color_control_enhanced_color_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::EnhancedColorMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_loop_active) {
    if (SL_STATUS_OK == dotdot_color_control_color_loop_active_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorLoopActive under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_loop_direction) {
    if (SL_STATUS_OK == dotdot_color_control_color_loop_direction_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorLoopDirection under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_loop_time) {
    if (SL_STATUS_OK == dotdot_color_control_color_loop_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorLoopTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_loop_start_enhanced_hue) {
    if (SL_STATUS_OK == dotdot_color_control_color_loop_start_enhanced_hue_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorLoopStartEnhancedHue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_loop_stored_enhanced_hue) {
    if (SL_STATUS_OK == dotdot_color_control_color_loop_stored_enhanced_hue_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorLoopStoredEnhancedHue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_capabilities) {
    if (SL_STATUS_OK == dotdot_color_control_color_capabilities_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorCapabilities under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_temp_physical_min_mireds) {
    if (SL_STATUS_OK == dotdot_color_control_color_temp_physical_min_mireds_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorTempPhysicalMinMireds under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.color_temp_physical_max_mireds) {
    if (SL_STATUS_OK == dotdot_color_control_color_temp_physical_max_mireds_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::ColorTempPhysicalMaxMireds under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.couple_color_temp_to_level_min_mireds) {
    if (SL_STATUS_OK == dotdot_color_control_couple_color_temp_to_level_min_mireds_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::CoupleColorTempToLevelMinMireds under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.start_up_color_temperature_mireds) {
    if (SL_STATUS_OK == dotdot_color_control_start_up_color_temperature_mireds_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ColorControl::StartUpColorTemperatureMireds under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster BallastConfiguration
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_ballast_configuration_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_ballast_configuration_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_ballast_configuration_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.physical_min_level) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_physical_min_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::PhysicalMinLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_max_level) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_physical_max_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::PhysicalMaxLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ballast_status) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_ballast_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::BallastStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_level) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_min_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::MinLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_level) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_max_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::MaxLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_on_level) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_power_on_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::PowerOnLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_on_fade_time) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_power_on_fade_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::PowerOnFadeTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.intrinsic_ballast_factor) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_intrinsic_ballast_factor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::IntrinsicBallastFactor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ballast_factor_adjustment) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_ballast_factor_adjustment_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::BallastFactorAdjustment under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_quantity) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_quantity_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampQuantity under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_type) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_manufacturer) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_manufacturer_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampManufacturer under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_rated_hours) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_rated_hours_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampRatedHours under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_burn_hours) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_burn_hours_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampBurnHours under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_alarm_mode) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_alarm_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampAlarmMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.lamp_burn_hours_trip_point) {
    if (SL_STATUS_OK == dotdot_ballast_configuration_lamp_burn_hours_trip_point_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of BallastConfiguration::LampBurnHoursTripPoint under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster IlluminanceMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_illuminance_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_illuminance_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_illuminance_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_illuminance_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_illuminance_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_illuminance_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_illuminance_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.light_sensor_type) {
    if (SL_STATUS_OK == dotdot_illuminance_measurement_light_sensor_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceMeasurement::LightSensorType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster IlluminanceLevelSensing
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_illuminance_level_sensing_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_illuminance_level_sensing_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_illuminance_level_sensing_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.level_status) {
    if (SL_STATUS_OK == dotdot_illuminance_level_sensing_level_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceLevelSensing::LevelStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.light_sensor_type) {
    if (SL_STATUS_OK == dotdot_illuminance_level_sensing_light_sensor_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceLevelSensing::LightSensorType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.illuminance_target_level) {
    if (SL_STATUS_OK == dotdot_illuminance_level_sensing_illuminance_target_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IlluminanceLevelSensing::IlluminanceTargetLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster TemperatureMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_temperature_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_temperature_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_temperature_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_temperature_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of TemperatureMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_temperature_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of TemperatureMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_temperature_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of TemperatureMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_temperature_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of TemperatureMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PressureMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_pressure_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_pressure_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_pressure_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.scaled_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_scaled_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::ScaledValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_scaled_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_min_scaled_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::MinScaledValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_scaled_value) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_max_scaled_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::MaxScaledValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.scaled_tolerance) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_scaled_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::ScaledTolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.scale) {
    if (SL_STATUS_OK == dotdot_pressure_measurement_scale_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PressureMeasurement::Scale under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster FlowMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_flow_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_flow_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_flow_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_flow_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FlowMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_flow_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FlowMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_flow_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FlowMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_flow_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of FlowMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster RelativityHumidity
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_relativity_humidity_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_relativity_humidity_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_relativity_humidity_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_relativity_humidity_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of RelativityHumidity::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_relativity_humidity_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of RelativityHumidity::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_relativity_humidity_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of RelativityHumidity::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_relativity_humidity_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of RelativityHumidity::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster OccupancySensing
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_occupancy_sensing_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_occupancy_sensing_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_occupancy_sensing_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.occupancy) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_occupancy_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::Occupancy under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupancy_sensor_type) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_occupancy_sensor_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::OccupancySensorType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.occupancy_sensor_type_bitmap) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_occupancy_sensor_type_bitmap_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::OccupancySensorTypeBitmap under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pir_occupied_to_unoccupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_pir_occupied_to_unoccupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PIROccupiedToUnoccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pir_unoccupied_to_occupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_pir_unoccupied_to_occupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PIRUnoccupiedToOccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.pir_unoccupied_to_occupied_threshold) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_pir_unoccupied_to_occupied_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PIRUnoccupiedToOccupiedThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ultrasonic_occupied_to_unoccupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_ultrasonic_occupied_to_unoccupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::UltrasonicOccupiedToUnoccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ultrasonic_unoccupied_to_occupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_ultrasonic_unoccupied_to_occupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::UltrasonicUnoccupiedToOccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ultrasonic_unoccupied_to_occupied_threshold) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_ultrasonic_unoccupied_to_occupied_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::UltrasonicUnoccupiedToOccupiedThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_contact_occupied_to_unoccupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_physical_contact_occupied_to_unoccupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PhysicalContactOccupiedToUnoccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_contact_unoccupied_to_occupied_delay) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_physical_contact_unoccupied_to_occupied_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PhysicalContactUnoccupiedToOccupiedDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.physical_contact_unoccupied_to_occupied_threshold) {
    if (SL_STATUS_OK == dotdot_occupancy_sensing_physical_contact_unoccupied_to_occupied_threshold_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of OccupancySensing::PhysicalContactUnoccupiedToOccupiedThreshold under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster SoilMoisture
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_soil_moisture_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_soil_moisture_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_soil_moisture_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_soil_moisture_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of SoilMoisture::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_soil_moisture_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of SoilMoisture::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_soil_moisture_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of SoilMoisture::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_soil_moisture_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of SoilMoisture::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PhMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_ph_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_ph_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_ph_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_ph_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PhMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_ph_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PhMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_ph_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PhMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_ph_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PhMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ElectricalConductivityMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_electrical_conductivity_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_electrical_conductivity_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_electrical_conductivity_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_electrical_conductivity_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalConductivityMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_electrical_conductivity_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalConductivityMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_electrical_conductivity_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalConductivityMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_electrical_conductivity_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalConductivityMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster WindSpeedMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_wind_speed_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_wind_speed_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_wind_speed_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_wind_speed_measurement_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindSpeedMeasurement::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_wind_speed_measurement_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindSpeedMeasurement::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_wind_speed_measurement_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindSpeedMeasurement::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_wind_speed_measurement_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of WindSpeedMeasurement::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster CarbonMonoxide
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_carbon_monoxide_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_carbon_monoxide_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_carbon_monoxide_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_monoxide_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonMonoxide::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_monoxide_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonMonoxide::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_monoxide_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonMonoxide::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_carbon_monoxide_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonMonoxide::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster CarbonDioxide
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_carbon_dioxide_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_carbon_dioxide_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_carbon_dioxide_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_dioxide_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonDioxide::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_dioxide_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonDioxide::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_carbon_dioxide_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonDioxide::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_carbon_dioxide_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of CarbonDioxide::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster PM25
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_pm25_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_pm25_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_pm25_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measured_value) {
    if (SL_STATUS_OK == dotdot_pm25_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PM25::MeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.min_measured_value) {
    if (SL_STATUS_OK == dotdot_pm25_min_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PM25::MinMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.max_measured_value) {
    if (SL_STATUS_OK == dotdot_pm25_max_measured_value_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PM25::MaxMeasuredValue under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.tolerance) {
    if (SL_STATUS_OK == dotdot_pm25_tolerance_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of PM25::Tolerance under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster IASZone
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_ias_zone_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_ias_zone_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_ias_zone_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.zone_state) {
    if (SL_STATUS_OK == dotdot_ias_zone_zone_state_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::ZoneState under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.zone_type) {
    if (SL_STATUS_OK == dotdot_ias_zone_zone_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::ZoneType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.zone_status) {
    if (SL_STATUS_OK == dotdot_ias_zone_zone_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::ZoneStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.iascie_address) {
    if (SL_STATUS_OK == dotdot_ias_zone_iascie_address_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::IASCIEAddress under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.zoneid) {
    if (SL_STATUS_OK == dotdot_ias_zone_zoneid_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::ZoneID under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.number_of_zone_sensitivity_levels_supported) {
    if (SL_STATUS_OK == dotdot_ias_zone_number_of_zone_sensitivity_levels_supported_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::NumberOfZoneSensitivityLevelsSupported under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_zone_sensitivity_level) {
    if (SL_STATUS_OK == dotdot_ias_zone_current_zone_sensitivity_level_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASZone::CurrentZoneSensitivityLevel under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster IASWD
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_iaswd_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_iaswd_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_iaswd_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.max_duration) {
    if (SL_STATUS_OK == dotdot_iaswd_max_duration_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of IASWD::MaxDuration under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Metering
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_metering_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_metering_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_metering_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.current_summation_delivered) {
    if (SL_STATUS_OK == dotdot_metering_current_summation_delivered_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentSummationDelivered under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_summation_received) {
    if (SL_STATUS_OK == dotdot_metering_current_summation_received_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentSummationReceived under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_max_demand_delivered) {
    if (SL_STATUS_OK == dotdot_metering_current_max_demand_delivered_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentMaxDemandDelivered under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_max_demand_received) {
    if (SL_STATUS_OK == dotdot_metering_current_max_demand_received_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentMaxDemandReceived under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_factor) {
    if (SL_STATUS_OK == dotdot_metering_power_factor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::PowerFactor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reading_snap_shot_time) {
    if (SL_STATUS_OK == dotdot_metering_reading_snap_shot_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::ReadingSnapShotTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_max_demand_delivered_time) {
    if (SL_STATUS_OK == dotdot_metering_current_max_demand_delivered_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentMaxDemandDeliveredTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_max_demand_received_time) {
    if (SL_STATUS_OK == dotdot_metering_current_max_demand_received_time_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentMaxDemandReceivedTime under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.default_update_period) {
    if (SL_STATUS_OK == dotdot_metering_default_update_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::DefaultUpdatePeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.supply_status) {
    if (SL_STATUS_OK == dotdot_metering_supply_status_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::SupplyStatus under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_inlet_energy_carrier_summation) {
    if (SL_STATUS_OK == dotdot_metering_current_inlet_energy_carrier_summation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentInletEnergyCarrierSummation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.current_outlet_energy_carrier_summation) {
    if (SL_STATUS_OK == dotdot_metering_current_outlet_energy_carrier_summation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::CurrentOutletEnergyCarrierSummation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.inlet_temperature) {
    if (SL_STATUS_OK == dotdot_metering_inlet_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::InletTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.outlet_temperature) {
    if (SL_STATUS_OK == dotdot_metering_outlet_temperature_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::OutletTemperature under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.unitof_measure) {
    if (SL_STATUS_OK == dotdot_metering_unitof_measure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::UnitofMeasure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.multiplier) {
    if (SL_STATUS_OK == dotdot_metering_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::Multiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.divisor) {
    if (SL_STATUS_OK == dotdot_metering_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::Divisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.summation_formatting) {
    if (SL_STATUS_OK == dotdot_metering_summation_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::SummationFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.demand_formatting) {
    if (SL_STATUS_OK == dotdot_metering_demand_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::DemandFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.historical_consumption_formatting) {
    if (SL_STATUS_OK == dotdot_metering_historical_consumption_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::HistoricalConsumptionFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.metering_device_type) {
    if (SL_STATUS_OK == dotdot_metering_metering_device_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::MeteringDeviceType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.energy_carrier_unit_of_measure) {
    if (SL_STATUS_OK == dotdot_metering_energy_carrier_unit_of_measure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::EnergyCarrierUnitOfMeasure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.energy_carrier_summation_formatting) {
    if (SL_STATUS_OK == dotdot_metering_energy_carrier_summation_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::EnergyCarrierSummationFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.energy_carrier_demand_formatting) {
    if (SL_STATUS_OK == dotdot_metering_energy_carrier_demand_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::EnergyCarrierDemandFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.temperature_unit_of_measure) {
    if (SL_STATUS_OK == dotdot_metering_temperature_unit_of_measure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::TemperatureUnitOfMeasure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.temperature_formatting) {
    if (SL_STATUS_OK == dotdot_metering_temperature_formatting_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Metering::TemperatureFormatting under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ElectricalMeasurement
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_electrical_measurement_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_electrical_measurement_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_electrical_measurement_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.measurement_type) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measurement_type_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasurementType under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltageMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltageMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrentMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrentMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCPower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_power_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_power_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCPowerMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_power_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_power_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCPowerMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltageMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltageDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrentMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrentDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_power_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_power_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCPowerMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_power_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_power_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCPowerDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_frequency) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_frequency_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACFrequency under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_frequency_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_frequency_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACFrequencyMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_frequency_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_frequency_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACFrequencyMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.neutral_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_neutral_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::NeutralCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.total_active_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_total_active_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::TotalActivePower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.total_reactive_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_total_reactive_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::TotalReactivePower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.total_apparent_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_total_apparent_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::TotalApparentPower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured1st_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured1st_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured1stHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured3rd_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured3rd_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured3rdHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured5th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured5th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured5thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured7th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured7th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured7thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured9th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured9th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured9thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured11th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured11th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::Measured11thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase1st_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase1st_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase1stHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase3rd_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase3rd_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase3rdHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase5th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase5th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase5thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase7th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase7th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase7thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase9th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase9th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase9thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.measured_phase11th_harmonic_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_measured_phase11th_harmonic_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::MeasuredPhase11thHarmonicCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_frequency_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_frequency_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACFrequencyMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_frequency_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_frequency_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACFrequencyDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_power_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PowerMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_power_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PowerDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.harmonic_current_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_harmonic_current_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::HarmonicCurrentMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.phase_harmonic_current_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_phase_harmonic_current_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PhaseHarmonicCurrentMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.line_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_line_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::LineCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActiveCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactiveCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_min) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_min_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMin under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_max) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_max_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMax under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactivePower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.apparent_power) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_apparent_power_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ApparentPower under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_factor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_power_factor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PowerFactor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_voltage_measurement_period) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_voltage_measurement_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSVoltageMeasurementPeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_over_voltage_counter) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_over_voltage_counter_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSOverVoltageCounter under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_under_voltage_counter) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_under_voltage_counter_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSUnderVoltageCounter under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_over_voltage_period) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_over_voltage_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeOverVoltagePeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_under_voltage_period) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_under_voltage_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeUnderVoltagePeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_sag_period) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_sag_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSagPeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_swell_period) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_swell_period_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSwellPeriod under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_voltage_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_voltage_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACVoltageMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_voltage_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_voltage_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACVoltageDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_current_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_current_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACCurrentMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_current_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_current_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACCurrentDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_power_multiplier) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_power_multiplier_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACPowerMultiplier under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_power_divisor) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_power_divisor_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACPowerDivisor under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_overload_alarms_mask) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_overload_alarms_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCOverloadAlarmsMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_voltage_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_voltage_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCVoltageOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.dc_current_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_dc_current_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::DCCurrentOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_alarms_mask) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_alarms_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACAlarmsMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_voltage_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_voltage_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACVoltageOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_current_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_current_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACCurrentOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_active_power_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_active_power_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACActivePowerOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.ac_reactive_power_overload) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_ac_reactive_power_overload_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ACReactivePowerOverload under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_over_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_over_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSOverVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_under_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_under_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSUnderVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_over_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_over_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeOverVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_under_voltage) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_under_voltage_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeUnderVoltage under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_sag) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_sag_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSag under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_swell) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_swell_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSwell under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.line_current_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_line_current_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::LineCurrentPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_current_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_current_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActiveCurrentPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_current_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_current_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactiveCurrentPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltagePhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_min_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_min_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMinPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_max_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_max_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMaxPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_min_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_min_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMinPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_max_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_max_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMaxPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_min_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_min_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMinPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_max_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_max_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMaxPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_power_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_power_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactivePowerPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.apparent_power_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_apparent_power_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ApparentPowerPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_factor_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_power_factor_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PowerFactorPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_voltage_measurement_period_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_voltage_measurement_period_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSVoltageMeasurementPeriodPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_over_voltage_counter_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_over_voltage_counter_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSOverVoltageCounterPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_under_voltage_counter_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_under_voltage_counter_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSUnderVoltageCounterPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_over_voltage_period_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_over_voltage_period_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeOverVoltagePeriodPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_under_voltage_period_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_under_voltage_period_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeUnderVoltagePeriodPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_sag_period_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_sag_period_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSagPeriodPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_swell_period_phb) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_swell_period_phb_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSwellPeriodPhB under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.line_current_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_line_current_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::LineCurrentPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_current_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_current_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActiveCurrentPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_current_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_current_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactiveCurrentPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltagePhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_min_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_min_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMinPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_max_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_max_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageMaxPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_min_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_min_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMinPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_current_max_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_current_max_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSCurrentMaxPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_min_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_min_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMinPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.active_power_max_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_active_power_max_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ActivePowerMaxPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.reactive_power_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_reactive_power_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ReactivePowerPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.apparent_power_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_apparent_power_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::ApparentPowerPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.power_factor_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_power_factor_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::PowerFactorPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_voltage_measurement_period_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_voltage_measurement_period_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSVoltageMeasurementPeriodPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_over_voltage_counter_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_over_voltage_counter_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSOverVoltageCounterPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_rms_under_voltage_counter_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_average_rms_under_voltage_counter_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::AverageRMSUnderVoltageCounterPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_over_voltage_period_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_over_voltage_period_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeOverVoltagePeriodPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_extreme_under_voltage_period_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_extreme_under_voltage_period_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSExtremeUnderVoltagePeriodPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_sag_period_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_sag_period_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSagPeriodPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.rms_voltage_swell_period_phc) {
    if (SL_STATUS_OK == dotdot_electrical_measurement_rms_voltage_swell_period_phc_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ElectricalMeasurement::RMSVoltageSwellPeriodPhC under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Diagnostics
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_diagnostics_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_diagnostics_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_diagnostics_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.number_of_resets) {
    if (SL_STATUS_OK == dotdot_diagnostics_number_of_resets_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NumberOfResets under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.persistent_memory_writes) {
    if (SL_STATUS_OK == dotdot_diagnostics_persistent_memory_writes_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::PersistentMemoryWrites under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_rx_bcast) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_rx_bcast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacRxBcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_tx_bcast) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_tx_bcast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacTxBcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_rx_ucast) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_rx_ucast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacRxUcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_tx_ucast) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_tx_ucast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacTxUcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_tx_ucast_retry) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_tx_ucast_retry_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacTxUcastRetry under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.mac_tx_ucast_fail) {
    if (SL_STATUS_OK == dotdot_diagnostics_mac_tx_ucast_fail_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::MacTxUcastFail under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_rx_bcast) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_rx_bcast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSRxBcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_tx_bcast) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_tx_bcast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSTxBcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_rx_ucast) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_rx_ucast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSRxUcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_tx_ucast_success) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_tx_ucast_success_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSTxUcastSuccess under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_tx_ucast_retry) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_tx_ucast_retry_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSTxUcastRetry under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_tx_ucast_fail) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_tx_ucast_fail_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSTxUcastFail under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.route_disc_initiated) {
    if (SL_STATUS_OK == dotdot_diagnostics_route_disc_initiated_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::RouteDiscInitiated under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.neighbor_added) {
    if (SL_STATUS_OK == dotdot_diagnostics_neighbor_added_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NeighborAdded under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.neighbor_removed) {
    if (SL_STATUS_OK == dotdot_diagnostics_neighbor_removed_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NeighborRemoved under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.neighbor_stale) {
    if (SL_STATUS_OK == dotdot_diagnostics_neighbor_stale_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NeighborStale under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.join_indication) {
    if (SL_STATUS_OK == dotdot_diagnostics_join_indication_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::JoinIndication under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.child_moved) {
    if (SL_STATUS_OK == dotdot_diagnostics_child_moved_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::ChildMoved under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.nwkfc_failure) {
    if (SL_STATUS_OK == dotdot_diagnostics_nwkfc_failure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NWKFCFailure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.apsfc_failure) {
    if (SL_STATUS_OK == dotdot_diagnostics_apsfc_failure_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSFCFailure under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_unauthorized_key) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_unauthorized_key_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSUnauthorizedKey under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.nwk_decrypt_failures) {
    if (SL_STATUS_OK == dotdot_diagnostics_nwk_decrypt_failures_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::NWKDecryptFailures under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aps_decrypt_failures) {
    if (SL_STATUS_OK == dotdot_diagnostics_aps_decrypt_failures_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::APSDecryptFailures under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.packet_buffer_allocate_failures) {
    if (SL_STATUS_OK == dotdot_diagnostics_packet_buffer_allocate_failures_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::PacketBufferAllocateFailures under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.relayed_ucast) {
    if (SL_STATUS_OK == dotdot_diagnostics_relayed_ucast_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::RelayedUcast under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.phy_to_mac_queue_limit_reached) {
    if (SL_STATUS_OK == dotdot_diagnostics_phy_to_mac_queue_limit_reached_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::PHYToMACQueueLimitReached under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.packet_validate_drop_count) {
    if (SL_STATUS_OK == dotdot_diagnostics_packet_validate_drop_count_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::PacketValidateDropCount under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.average_mac_retry_per_aps_message_sent) {
    if (SL_STATUS_OK == dotdot_diagnostics_average_mac_retry_per_aps_message_sent_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::AverageMACRetryPerAPSMessageSent under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.last_messagelqi) {
    if (SL_STATUS_OK == dotdot_diagnostics_last_messagelqi_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::LastMessageLQI under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.last_messagerssi) {
    if (SL_STATUS_OK == dotdot_diagnostics_last_messagerssi_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Diagnostics::LastMessageRSSI under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Binding
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_binding_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_binding_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_binding_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.binding_table_full) {
    if (SL_STATUS_OK == dotdot_binding_binding_table_full_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Binding::BindingTableFull under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.bindable_cluster_list) {
    if (SL_STATUS_OK == dotdot_binding_bindable_cluster_list_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Binding::BindableClusterList under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.binding_table) {
    if (SL_STATUS_OK == dotdot_binding_binding_table_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Binding::BindingTable under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster NameAndLocation
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_name_and_location_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_name_and_location_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_name_and_location_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.name) {
    if (SL_STATUS_OK == dotdot_name_and_location_name_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of NameAndLocation::Name under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.location) {
    if (SL_STATUS_OK == dotdot_name_and_location_location_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of NameAndLocation::Location under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster ConfigurationParameters
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_configuration_parameters_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_configuration_parameters_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_configuration_parameters_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.configuration_parameters) {
    if (SL_STATUS_OK == dotdot_configuration_parameters_configuration_parameters_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of ConfigurationParameters::ConfigurationParameters under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster AoXLocator
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_aox_locator_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_aox_locator_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_aox_locator_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.reporting_mode) {
    if (SL_STATUS_OK == dotdot_aox_locator_reporting_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::ReportingMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.position_and_orientation_valid) {
    if (SL_STATUS_OK == dotdot_aox_locator_position_and_orientation_valid_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::PositionAndOrientationValid under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.position_and_orientation) {
    if (SL_STATUS_OK == dotdot_aox_locator_position_and_orientation_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::PositionAndOrientation under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.azimuth_mask) {
    if (SL_STATUS_OK == dotdot_aox_locator_azimuth_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AzimuthMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.elevation_mask) {
    if (SL_STATUS_OK == dotdot_aox_locator_elevation_mask_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::ElevationMask under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.allow_list) {
    if (SL_STATUS_OK == dotdot_aox_locator_allow_list_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AllowList under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.aox_mode) {
    if (SL_STATUS_OK == dotdot_aox_locator_aox_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AoXMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.antenna_mode) {
    if (SL_STATUS_OK == dotdot_aox_locator_antenna_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AntennaMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.antenna_array) {
    if (SL_STATUS_OK == dotdot_aox_locator_antenna_array_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AntennaArray under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.period_samples) {
    if (SL_STATUS_OK == dotdot_aox_locator_period_samples_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::PeriodSamples under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.angle_filtering) {
    if (SL_STATUS_OK == dotdot_aox_locator_angle_filtering_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AngleFiltering under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.angle_filtering_weight) {
    if (SL_STATUS_OK == dotdot_aox_locator_angle_filtering_weight_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AngleFilteringWeight under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.angle_correction_timeout) {
    if (SL_STATUS_OK == dotdot_aox_locator_angle_correction_timeout_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AngleCorrectionTimeout under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.angle_correction_delay) {
    if (SL_STATUS_OK == dotdot_aox_locator_angle_correction_delay_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::AngleCorrectionDelay under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.cte_mode) {
    if (SL_STATUS_OK == dotdot_aox_locator_cte_mode_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::CTEMode under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.cte_sampling_interval) {
    if (SL_STATUS_OK == dotdot_aox_locator_cte_sampling_interval_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::CTESamplingInterval under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.cte_length) {
    if (SL_STATUS_OK == dotdot_aox_locator_cte_length_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::CTELength under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  if (true == attribute_list.slot_duration) {
    if (SL_STATUS_OK == dotdot_aox_locator_slot_duration_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXLocator::SlotDuration under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster AoXPositionEstimation
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_aox_position_estimation_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_aox_position_estimation_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_aox_position_estimation_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.position) {
    if (SL_STATUS_OK == dotdot_aox_position_estimation_position_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of AoXPositionEstimation::Position under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
////////////////////////////////////////////////////////////////////////////////
// Start of cluster Descriptor
////////////////////////////////////////////////////////////////////////////////
static sl_status_t uic_mqtt_dotdot_descriptor_force_read_attributes_callback (
  const dotdot_unid_t unid,
  dotdot_endpoint_id_t endpoint_id,
  uic_mqtt_dotdot_callback_call_type_t call_type,
  uic_mqtt_dotdot_descriptor_updated_state_t attribute_list) {

  if (false == is_force_read_attributes_enabled()){
    return SL_STATUS_FAIL;
  }

  if (call_type == UIC_MQTT_DOTDOT_CALLBACK_TYPE_SUPPORT_CHECK) {
    if (is_automatic_deduction_of_supported_commands_enabled()) {
      return dotdot_is_any_descriptor_attribute_supported(unid, endpoint_id) ?
        SL_STATUS_OK : SL_STATUS_FAIL;
    } else {
      return SL_STATUS_FAIL;
    }
  }

  // Go and undefine everything that needs to be read again:
  if (true == attribute_list.device_type_list) {
    if (SL_STATUS_OK == dotdot_descriptor_device_type_list_undefine_reported(unid, endpoint_id)) {
      sl_log_debug(LOG_TAG, "Undefined Reported value of Descriptor::DeviceTypeList under %s - Endpoint %d", unid, endpoint_id);
    }
  }
  return SL_STATUS_OK;
}
// clang-format on

////////////////////////////////////////////////////////////////////////////////
// Functions shared within the component
////////////////////////////////////////////////////////////////////////////////
sl_status_t
  unify_dotdot_attribute_store_force_read_attributes_command_callbacks_init()
{
  // Register ForceReadAttribute callbacks on all supported clusters
  // clang-format off
  
  uic_mqtt_dotdot_set_basic_force_read_attributes_callback(&uic_mqtt_dotdot_basic_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_power_configuration_force_read_attributes_callback(&uic_mqtt_dotdot_power_configuration_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_device_temperature_configuration_force_read_attributes_callback(&uic_mqtt_dotdot_device_temperature_configuration_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_identify_force_read_attributes_callback(&uic_mqtt_dotdot_identify_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_groups_force_read_attributes_callback(&uic_mqtt_dotdot_groups_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_scenes_force_read_attributes_callback(&uic_mqtt_dotdot_scenes_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_on_off_force_read_attributes_callback(&uic_mqtt_dotdot_on_off_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_level_force_read_attributes_callback(&uic_mqtt_dotdot_level_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_alarms_force_read_attributes_callback(&uic_mqtt_dotdot_alarms_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_time_force_read_attributes_callback(&uic_mqtt_dotdot_time_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_poll_control_force_read_attributes_callback(&uic_mqtt_dotdot_poll_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_shade_configuration_force_read_attributes_callback(&uic_mqtt_dotdot_shade_configuration_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_door_lock_force_read_attributes_callback(&uic_mqtt_dotdot_door_lock_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_window_covering_force_read_attributes_callback(&uic_mqtt_dotdot_window_covering_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_barrier_control_force_read_attributes_callback(&uic_mqtt_dotdot_barrier_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_pump_configuration_and_control_force_read_attributes_callback(&uic_mqtt_dotdot_pump_configuration_and_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_thermostat_force_read_attributes_callback(&uic_mqtt_dotdot_thermostat_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_fan_control_force_read_attributes_callback(&uic_mqtt_dotdot_fan_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_dehumidification_control_force_read_attributes_callback(&uic_mqtt_dotdot_dehumidification_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_thermostat_user_interface_configuration_force_read_attributes_callback(&uic_mqtt_dotdot_thermostat_user_interface_configuration_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_color_control_force_read_attributes_callback(&uic_mqtt_dotdot_color_control_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_ballast_configuration_force_read_attributes_callback(&uic_mqtt_dotdot_ballast_configuration_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_illuminance_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_illuminance_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_illuminance_level_sensing_force_read_attributes_callback(&uic_mqtt_dotdot_illuminance_level_sensing_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_temperature_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_temperature_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_pressure_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_pressure_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_flow_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_flow_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_relativity_humidity_force_read_attributes_callback(&uic_mqtt_dotdot_relativity_humidity_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_occupancy_sensing_force_read_attributes_callback(&uic_mqtt_dotdot_occupancy_sensing_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_soil_moisture_force_read_attributes_callback(&uic_mqtt_dotdot_soil_moisture_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_ph_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_ph_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_electrical_conductivity_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_electrical_conductivity_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_wind_speed_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_wind_speed_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_carbon_monoxide_force_read_attributes_callback(&uic_mqtt_dotdot_carbon_monoxide_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_carbon_dioxide_force_read_attributes_callback(&uic_mqtt_dotdot_carbon_dioxide_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_pm25_force_read_attributes_callback(&uic_mqtt_dotdot_pm25_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_ias_zone_force_read_attributes_callback(&uic_mqtt_dotdot_ias_zone_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_iaswd_force_read_attributes_callback(&uic_mqtt_dotdot_iaswd_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_metering_force_read_attributes_callback(&uic_mqtt_dotdot_metering_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_electrical_measurement_force_read_attributes_callback(&uic_mqtt_dotdot_electrical_measurement_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_diagnostics_force_read_attributes_callback(&uic_mqtt_dotdot_diagnostics_force_read_attributes_callback);
  


  uic_mqtt_dotdot_set_binding_force_read_attributes_callback(&uic_mqtt_dotdot_binding_force_read_attributes_callback);
  


  uic_mqtt_dotdot_set_name_and_location_force_read_attributes_callback(&uic_mqtt_dotdot_name_and_location_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_configuration_parameters_force_read_attributes_callback(&uic_mqtt_dotdot_configuration_parameters_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_aox_locator_force_read_attributes_callback(&uic_mqtt_dotdot_aox_locator_force_read_attributes_callback);
  
  uic_mqtt_dotdot_set_aox_position_estimation_force_read_attributes_callback(&uic_mqtt_dotdot_aox_position_estimation_force_read_attributes_callback);
  

  uic_mqtt_dotdot_set_descriptor_force_read_attributes_callback(&uic_mqtt_dotdot_descriptor_force_read_attributes_callback);
  
  // clang-format on

  return SL_STATUS_OK;
}
